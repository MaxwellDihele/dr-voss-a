import { defineConfig } from 'astro/config';

export default defineConfig({
  // Vercel adapter can be added here: @astrojs/vercel/serverless
  // Add integrations like React, Tailwind, MDX as needed
});
